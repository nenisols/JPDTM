<!DOCTYPE html>
<html>
<head>
  <title> Passport API Client Login</title>
    <link href = "https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css" rel="stylesheet" />
    <link href = "http://getbootstrap.com/assets/css/docs.min.css" rel="stylesheet" />
    <link href = "http://bootstrapdocs.com/v3.2.0/docs/examples/dashboard/dashboard.css" rel="stylesheet" />
    <link href = "http://bootstrapdocs.com/v3.2.0/docs/examples/sticky-footer/sticky-footer.css" rel="stylesheet" />
    
    {% block stylesheets %}
    
    {% stylesheets '@ApiClientBundle/Resources/public/less/login.less' filter='less' %}
        <link href="{{ asset_url }}" type="text/css" rel="stylesheet" />
    {% endstylesheets %}
    {% endblock %}  
</head>
<body>

<div class="container">
    <div class="row">
        <div class="col-md-8 col-md-offset-2">
            <h1 class="text-center login-title">Passport API Client Application</h1>
            <div class="account-wall">
                {% for flashMessage in app.session.flashbag.get('error') %}
                    <div class="alert alert-danger fade in">
                        <a href="#" class="close" data-dismiss="alert">&times;</a>
                        <strong>Error!</strong> {{ flashMessage }}
                    </div>                      
                {% endfor %}

                {% for flashMessage in app.session.flashbag.get('success') %}
                    <div class="alert alert-success fade in">
                        <a href="#" class="close" data-dismiss="alert">&times;</a>
                        <strong>Success!</strong> {{ flashMessage }}
                    </div>   
                {% endfor %}
                <div class="col-md-8 col-md-offset-5">
                  <h1><i class="glyphicon glyphicon-user "></i></h1>
                </div>

              <form class="form-signin" action="{{ path('login_check') }}" method="post">
                    <legend>Login</legend>
                    {% if error %}
                      <div class="alert alert-danger" >{{ error.messageKey|trans(error.messageData) }}</div>
                    {% endif %}
                    <fieldset>
                        <div class="form-group">
                          <input type="text" class="form-control" name="_username" id="email" placeholder="E-mail" value="{{ last_username }}" autofocus />
                        </div>
                        <div class="form-group">
                          <input type="password" class="form-control" name="_password" id="password" placeholder="Password" />
                        </div>
                        <button name="Login" type="submit" class="btn btn-primary">Login</button>
                        <label class="checkbox-inline">
                          <input type="checkbox" name="_remember_me" value="" />Keep me logged in
                        </label>
                    </fieldset>
                    <a href="#" class="pull-right need-help">Need help? </a><span class="clearfix"></span>
              </form>
            </div>
           {#  <a href="#" class="text-center new-account">Create an account </a> #}
        </div>
    </div>
</div>
</body>
</html>
